apiVersion: apps/v1
kind: Deployment
metadata:
  name: faasopt-deployment
  labels:
    app: faas-optimizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: faas-optimizer
  template:
    metadata:
      labels:
        app: faas-optimizer
    spec:
      containers:
      - name: faas-optimizer
        image: davideelliot/faas-optimizer:0.1
        ports:
        - containerPort: 4000
